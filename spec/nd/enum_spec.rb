# frozen_string_literal: true

RSpec.describe Nd::Enum do
  it "has a version number" do
    expect(Nd::Enum::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
